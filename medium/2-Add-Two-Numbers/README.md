using LinkedLists

at start of the while loop adding the carry so that from second step on it gets added before the actual addition happens and is not skipped. then we iterate thru both the linked lists from start only and add the digits. 

then we separate sum from carry and add sum to new node and carry is carried forward. base case of 1 is added at the end.
